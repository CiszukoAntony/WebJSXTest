const HeroTitle = ({title}) => {
  return (
    <div className="hero-title-class" id="hero-title-id">
        <p className="hero-title-text-class" id="hero-title-text-id">
            {title}
        </p>
    </div>
  )
}

export default HeroTitle