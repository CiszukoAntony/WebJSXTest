import '../globals.css'
// Importas el componente (la extensión .jsx al final es opcional, se suele omitir)

const Form = () => {
  return (
    <div>
        <p>
            Formulario Page.
            </p>
    </div>
  )
}

export default Form